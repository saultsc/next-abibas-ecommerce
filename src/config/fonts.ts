import { Inter, Montserrat, Montserrat_Alternates } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const titleFonts = Montserrat_Alternates({ subsets: ['latin'], weight: ['500', '700'] });

export const comicSans = Montserrat({ subsets: ['latin'], weight: ['400', '700'] });
